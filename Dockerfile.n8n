FROM n8nio/n8n:stable

USER root

# Debian, não Alpine
RUN apt-get update && \
    apt-get install -y docker.io curl && \
    apt-get clean && rm -rf /var/lib/apt/lists/*

# Copia e instala dd-trace localmente
COPY ./n8n/data /tracer
RUN cd /tracer && npm install

USER node
